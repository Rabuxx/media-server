{{- range .Values.services }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .name }}
spec:
  selector:
    app: {{ .name }}
  type: NodePort
  ports:
    - port: {{ .port }}
      targetPort: {{ .port }}
      nodePort: {{ .nodePort }}
---
{{- end }}
